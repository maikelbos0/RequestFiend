<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:RequestFiend.UI"
             x:Class="RequestFiend.UI.NewRequestTemplateCollectionPage"
             x:DataType="local:NewRequestTemplateCollection">

    <ScrollView>
        <VerticalStackLayout
            Padding="25"
            Spacing="25">

            <Label
                Text="New collection"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />
            
            <Entry Text="{Binding Name, Mode=TwoWay}" />

            <Button Text="Create new collection" Clicked="OnCreateClicked"></Button>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
